# ローカル開発とdocker-compose用の環境変数例
# UID/GID/USERNAME/GROUPNAMEはdevcontainer Dockerfileでコンテナ内に
# 対応するユーザーを作成するために使用されます。デフォルトはUID=1000と"node"です。

# ユーザー/グループ設定
UID=1000
GID=1000
USERNAME=node
GROUPNAME=node

# その他のランタイム環境変数
NODE_ENV=development

# cdktf用変数: 開発環境=dev、本番環境=prod
ENVIRONMENT=prod

# AWSリージョン（未設定時はコードのデフォルト値を使用）
AWS_REGION=ap-northeast-1

# AWS認証情報
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_SESSION_TOKEN=your-session-token-if-using-temporary-credentials

# 管理対象の既存AWS Security Group ID（インポート対象）
# IaCはこのSGに 80ポートでCloudflare IPのみ許可するように管理します。
SG_ID=sg-0abc1234def567890

# Google Cloud認証情報（CI/CD推奨）
# サービスアカウントキーのJSON文字列を直接指定
GOOGLE_CREDENTIALS={"type": "service_account", "project_id": "your-project", ...}

# Google Cloud認証情報（ローカル開発用）
# サービスアカウントキーファイルのパス（代替方法）
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
